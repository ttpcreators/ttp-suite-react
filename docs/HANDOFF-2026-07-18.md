# Passation — état au 2026-07-18

Point de reprise pour **le prochain assistant (ou une nouvelle session)**. Résume ce qui a
été fait, l'état courant, et ce qui reste ouvert. Pour le contexte technique de fond, lire
d'abord [`CLAUDE.md`](../CLAUDE.md), [`README.md`](../README.md), le runbook SQL
[`supabase/sql/README.md`](../supabase/sql/README.md) et [`docs/ARCHITECTURE.md`](ARCHITECTURE.md).

- **App** (`ttp-suite-react`) : prod `main` → https://app.ttpcreators.pro (déploiement auto ~1-2 min au push).
- **Vitrine** (`websitettpcreators`) : prod `main` → https://ttpcreators.pro (héberge les media kits publics + le PDF).
- Les deux repos partagent le **même projet Supabase** `zizvggziggswhrbuyhuo`.
- **Tag de sauvegarde de cette session : `handoff-2026-07-18`** (sur les deux repos).

> ⚠️ Règle prod inchangée : l'assistant n'a que la **clé anon** → il prépare le SQL / les
> déploiements de fonctions, **l'utilisateur les applique**. Toujours `npm run build`
> (oxlint + vitest + tsc + vite) **avant** tout commit.

---

## Ce qui a été livré cette session (tout déployé)

### Nouveaux modules
- **Gifting** (`src/views/Gifting.tsx`, `src/lib/gifting.ts`, onglet dans `CreatorSpace.tsx`) —
  suivi des cadeaux/dotations marques (agence + créateur) : marque, produit, valeur, **email
  de l'interlocuteur** (traçabilité), **contenu attendu ou non**, statut, rappel des mentions
  obligatoires (loi 2023-451). Table `gifting` (RLS motif briefs). **SQL appliqué + vérifié** ✅
  (voir runbook). Fiche détail au clic. Nav : Collaborations → Gifting.
- **Calculateur d'engagement** dans Debrief (`src/lib/engagement.ts` + tests, `src/views/DebriefCalculator.tsx`) —
  saisie des chiffres relevés sur les captures d'insights → taux d'engagement (2 bases :
  couverture / abonnés). Captures jointes (bucket privé `documents`, URL signées). Annexe dans
  le PDF/email. Fiche détail Debrief au clic.

### Contrats — durcissement juridique (droit FR, vérifié Légifrance)
`src/views/Contrats.tsx` : droit d'auteur ≠ droit à l'image (clauses séparées), cession
per-livrable (L131-1/L131-3), droit moral (L121-1), clause d'indépendance (risque mannequin
L7123-3/-4), transparence (loi 2023-451), force majeure, **clause vigilance URSSAF auto ≥ 5 000 € HT**.
Détails + citations dans la mémoire `ttp-contrats-droit-fr`. ⚠️ **Reste à revérifier** (agents
tombés sur limite de session) : loi 2023-451 art. 8 (clauses obligatoires + seuil décret) et
seuils **TVA 293 B** 2025-2026 — non hard-codés en attendant.

### Navigation / UX
- **Sidebar réorganisée** en 7 sections métier (`src/lib/nav.tsx`) : Pilotage · Créateurs ·
  Collaborations · **Finance** (isolée) · Relations · **Ressources** · **Réglages**.
- **Sections ouvertes par défaut** (sous-pages visibles, plus aéré), repliables + état mémorisé
  (localStorage `ttp:sb-group:<id>`).
- **3e niveau de nav (sous-pages)** pour les pages à onglets internes : Media kit →
  Créatrices/Agence/Fichiers, Contrats → les 3 types. Routage via `src/lib/navSub.ts`
  (`NavSubContext` + `useNavSub`) : App pose le sous-onglet, la vue le lit à son montage.
- **Multi-pages découvrable** : le système d'onglets + vue partagée existait déjà (clic droit) ;
  ajout d'un bouton **« ouvrir à côté »** (icône colonnes) révélé au survol de chaque page.
- **Media kit unifié** : « Media kit » et « Media kit agence » faisaient doublon → **une seule
  page à 3 onglets** (Créatrices/Agence/Fichiers). `AgencyTab` exporté de `MediakitAgence.tsx`.
- **Fiches détail au clic** ajoutées : Debrief, Gifting, Facturation (→ aperçu facture).
- **Checklist** : bug de suppression corrigé (la confirmation n'était pas rendue en vue liste)
  + 3 vues (cartes/liste/tableau).
- **Todo** : switch direct « Tous · Agence » + agencement des filtres en 2 rangées.
- **Documents** : les contrats `.html` s'affichaient en code source (accents cassés) car
  Supabase les sert en texte brut → rendu via **iframe `sandbox`** (sécurité + lisibilité).

### Performance
`src/views/charts/` (RevenueArea, FollowerArea lazy) — recharts (~101 Ko gzip) sorti du
premier écran (Aperçu + CreatorSpace). `vite.config.ts` : `manualChunks` React/motion (cache
stable). `src/lib/useLive.ts` : garde anti-rebond 5 s (fin du double-fetch focus+visibility).
`index` : 463 → 179 Ko.

### Revue de bugs
Revue adversariale du code de la session → 8 défauts réels corrigés (calculateur : remount,
mode global, parseNum ; Gifting : revert statut ; Documents : sécurité iframe…). Voir commit
`43a497c`.

### Vitrine (`websitettpcreators`)
Media kit agence : titre de couverture aéré (line-height) + logos marques agrandis (web + PDF).

### Ajouts de fin de session (déployés)
- **Media kit créateur — « Captures de stats » (6 max)** : nouveau champ `statsShots` dans le
  blob `creators.mediakit` (`MediakitEditor.tsx`), uploads via `ImageField` (bucket public
  `avatars`). Rendu côté vitrine en section **( 04 ) — Statistiques** (`mediakit.js` +
  `mediakit.css`), masquée si vide ; `object-fit: contain` pour ne pas rogner les chiffres ;
  PDF calibré 16:9. Marques/Contact renumérotés 05/06.
- **Espace créateur — sidebar groupée** : `CREATOR_GROUPS` est désormais la **source unique**
  (menu desktop via le même `SidebarNav` que l'agence, rail replié, nav mobile) → les trois ne
  peuvent plus diverger. Sections : Mon espace · Mon travail · Ressources. Ids préfixés `cs-`
  pour ne pas partager l'état d'ouverture (localStorage) avec l'agence.
- **Espace créateur — onglet « Media kit »** : la créatrice consulte sa page publique générée
  (aperçu intégré + copier le lien + ouvrir) et les fichiers déposés par l'agence. Pas de lien
  PDF en dur : le PDF est **versionné** (`media-kit-<build>.pdf`) — le bouton de téléchargement
  vit dans la page publique elle-même.
- **Espace créateur — Idées en 2 vues** : Liste ↔ Colonnes (kanban par statut), mémorisé ;
  carte factorisée (`ideaCard`) partagée par les deux vues.
- **Briefs remplissables par le créateur** : il écrit son script dans son espace → même colonne
  `consignes` que l'agence lit et exporte en PDF. Autorisé par la RLS `briefs_scoped` existante,
  **aucun SQL**. Écriture optimiste avec retour arrière + notification à l'agence.

### Ajouts du 2026-07-19 (déployés)
- **Dépôt de facture par le créateur** — il envoie sa facture si l'agence oublie de la saisir.
  ⚠️ **Deux protections de l'audit ont été PRÉSERVÉES, ne pas les défaire** : (1) le créateur
  n'écrit toujours pas dans `invoices` (sinon CA falsifiable) — il dépose un FICHIER que l'agence
  valide ; (2) son INSERT sur `documents` est contraint à `creator-uploads/<auth.uid()>/…` —
  sans cette contrainte de chemin il forgerait une ligne pointant vers le fichier d'une AUTRE
  créatrice, que la policy de lecture lui signerait (fuite inter-créateurs). SQL
  `sql/creator-depot-facture.sql` **appliqué + vérifié** (voir runbook).
- **PDF Brief & Debrief refondus** — `src/lib/pdfDoc.ts` = identité de document partagée
  (épurée, A4 : filets fins, bordeaux en touches, libellés mono vs corps sans).
  ⚠️ `debriefHTML` (Debrief.tsx) sert de **corps d'EMAIL** et reste en tableaux + styles en
  ligne : le PDF a sa fonction dédiée `debriefPdfHTML`. **Ne pas fusionner les deux.**
- **Logo TTP d'office** sur factures, contrats (marque/UGC + représentation) et PDF, via
  `ttpLogoUrl()` — URL **absolue** car les documents sont rendus dans un iframe `srcdoc`.
- **Templates** : bouton « Voir » (message entier en modale, le corps est tronqué en carte)
  + bascule Cartes / Liste mémorisée.

---

## 🔑 Accès — la seule chose que le repo ne transmet PAS

Le code, la doc et l'historique suffisent à comprendre le projet. Ce qui ne peut PAS passer
par GitHub et que **Marc doit fournir** au nouvel environnement :

| Accès | Sert à | Sans lui |
|---|---|---|
| **GitHub** (org/compte `ttpcreators`) | cloner, commiter, déclencher les déploiements | lecture seule / rien ne se déploie |
| **Dashboard Supabase** (projet `zizvggziggswhrbuyhuo`) | lancer le SQL, régler Auth, déployer les Edge Functions | les migrations restent en attente |
| **OVH** (DNS `ttpcreators.pro`) | DNS/domaine uniquement | sans objet au quotidien (⚠️ ne jamais toucher aux MX Google) |
| **Google / Gmail OAuth** | envoi d'emails via la fonction `gmail-send` | l'envoi Gmail échoue (repli Resend) |

> L'assistant n'a **que la clé anon** (publique par design). Il **prépare** le SQL / les
> commandes ; **Marc applique**. C'est la règle de prod, elle ne change pas.

## 🚀 Démarrage à froid (nouvel assistant)
1. Lire **ce fichier**, puis `CLAUDE.md`, puis `supabase/sql/README.md` (état des migrations).
2. `npm install` — porte de qualité obligatoire avant tout commit : `npm run build`
   (oxlint + vitest + tsc + vite).
3. Ne rien déployer sans comprendre : un `git push` sur `main` **met la prod à jour en 1-2 min**
   (app ET site vitrine).

---

## Ouvert / à décider (aucun bloquant)

1. **Contrats** — revérifier loi 2023-451 art. 8 + seuils TVA 293 B (ci-dessus).
2. **Deck media kit agence** — le **06 perso est encore public** sur la diapo Contact.
   Proposé : masquer le bloc Mobile quand le champ téléphone est vide (dans l'app) — **non fait**,
   en attente de feu vert. (Le champ téléphone est éditable dans l'app : Media kit → Agence.)
3. **Textes en dur du deck** (« LET'S WORK ! », bouton « Travaillons ensemble », intitulés) —
   pas encore éditables depuis l'app ; à rendre éditables ou changer sur demande.
4. **Photo d'agence** (diapo Contact du deck) — à uploader par l'utilisateur via l'app
   (Media kit → Agence → « Photo d'agence »). L'assistant ne peut pas uploader (clé anon).

## Comment reprendre
1. Lire ce fichier + `CLAUDE.md` + le runbook SQL (état des migrations).
2. La mémoire de l'assistant (`~/.claude/.../memory/MEMORY.md`) indexe le contexte projet
   (sécurité, media kit, contrats droit FR, audit finance en attente…).
3. `npm install && npm run dev`. Porte de qualité : `npm run build`.
